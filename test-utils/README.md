# test-utils/

Utilidades compartidas para pruebas (Vitest) en el monorepo.

- `vitestStrict.ts`: helpers/configuración para ejecutar pruebas con reglas más estrictas.

Uso típico: importado desde suites en `apps/*/tests`.
