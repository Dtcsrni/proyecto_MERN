{
  "name": "portal-alumno-cloud",
  "description": "API del portal alumno (solo lectura) para consulta de información y resultados; diseñada para despliegue cloud.",
  "version": "0.1.0",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/Dtcsrni/sistema-evaluacion-universitaria"
  },
  "keywords": ["api", "express", "typescript", "mongodb", "portal", "alumno"],
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --project tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "lint": "eslint . --ext .ts --max-warnings=0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0",
    "mongoose": "^9.1.3"
  }
}
