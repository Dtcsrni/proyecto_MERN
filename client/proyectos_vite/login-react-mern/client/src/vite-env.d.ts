/**
 * [BLOQUE DIDACTICO] client/src/vite-env.d.ts
 * Que es: punto de entrada de tipos generados por tooling Vite.
 * Que hace: habilita tipos de APIs del runtime Vite en el compilador TS.
 * Como lo hace: referencia el paquete `vite/client` para ampliar el contexto global.
 */

/// <reference types="vite/client" />
